# Source provenance

The repository history introduces `max_lock.go`, `metered_lock.go`, and their tests in commit `d9163aa6ee1369e7f4de76614f82c59dcda646e6`, titled `Import code`, on 2025-06-19. The two implementation files carry Weaviate B.V. notices, but that commit does not record an upstream repository, path, revision, or source URL.

GitHub code search did not find `MaxRWMutex` or `MeteredRWMutex` in a public repository under the Weaviate organization on 2026-07-09. That absence does not establish origin or ownership.

Before the first release, record the exact upstream source and revision for each imported file, confirm that the repository notices match that source, and retain or update notices based on that evidence. Do not remove the existing notices without the missing provenance record.
